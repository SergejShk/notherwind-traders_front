import styled from "styled-components";

export const ResultsTitle = styled.h2`
  font-size: 18px;
  line-height: 28px;
  color: #000;
  margin: 14px 0 8px;
`;

export const InfoListItem = styled.li`
  &:not(:last-child) {
    margin-bottom: 8px;
  }
`;

export const Info = styled.p`
  font-size: 14px;
  line-height: 20px;
  color: #9ca3af;
`;

export const NoResText = styled.p`
  margin-top: 25px;
`;
